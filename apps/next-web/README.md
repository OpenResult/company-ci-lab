# next-web

Minimal Next.js sample intended to demonstrate where application-specific quality checks, image build inputs, and deployment manifests live in the monorepo.

The scaffold includes lightweight scripts so `company-ci verify` can validate the project layout without requiring a full dependency install during every environment.
