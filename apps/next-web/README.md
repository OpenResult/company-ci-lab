# next-web

Minimal Node-focused web scaffold that keeps a Next-style app layout while producing an offline-buildable static artifact.

## Contracts

- `npm run lint` validates page/config consistency.
- `npm run test` verifies homepage copy expectations.
- `npm run build` renders `dist/index.html` and `dist/build-manifest.json`.
